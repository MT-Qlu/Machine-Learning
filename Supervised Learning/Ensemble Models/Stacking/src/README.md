# Source Code

Training and serving code for the Stacking Ensemble pipeline. Includes configuration helpers, data loaders, base learner and meta-learner training, and FastAPI inference adapters. The key component is the k-fold cross-validation mechanism that prevents data leakage when generating meta-features.
