# Source Code

Training and serving code for the Voting Ensemble pipeline. Includes both hard voting (majority class) and soft voting (average probabilities) implementations, configuration helpers, data loaders, and FastAPI inference adapters.
