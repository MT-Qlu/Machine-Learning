# Data

Local datasets and caches for the Ensemble Models › Bagging › Random Forest › Classification experiments. Ingestion code downloads or regenerates the files here; avoid storing secrets or raw PII. Commit `.gitkeep` so the structure stays reproducible.
