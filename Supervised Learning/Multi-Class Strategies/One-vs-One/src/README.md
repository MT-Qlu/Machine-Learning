# Source Code

Training and serving code for the One-vs-One multi-class strategy pipeline. Includes configuration helpers, data loaders (Iris), OvO classifier training with K(K-1)/2 binary classifiers, and FastAPI inference adapters. Demonstrates the balanced pairwise approach to multi-class problems, especially useful for imbalanced datasets.
