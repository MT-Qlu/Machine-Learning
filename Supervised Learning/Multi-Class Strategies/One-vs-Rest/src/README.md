# Source Code

Training and serving code for the One-vs-Rest multi-class strategy pipeline. Includes configuration helpers, data loaders (Iris), OvR classifier training, and FastAPI inference adapters. Demonstrates how binary classifiers like SVM are extended to multi-class problems.
