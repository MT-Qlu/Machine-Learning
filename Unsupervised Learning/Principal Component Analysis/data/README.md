# Data

Local datasets and caches for the Principal Component Analysis experiments. Ingestion code downloads or regenerates the files here; avoid storing secrets or raw PII. Commit `.gitkeep` so the structure stays reproducible.
